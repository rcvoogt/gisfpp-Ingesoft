<vbox spacing="6px" width="100%" height="100%">
	<div class="btn-toolbar pull-right">
		<a image="recursos/img/icon-createfile.png" tooltiptext="Crear y asignar una nueva Isfpp"
			class="btn btn-success btn-xs" onClick="@command('nuevaIsfpp')"
				disabled="@load(not vm.editando)"> Nueva Isfpp</a>
	</div>
	<grid hflex="1" vflex="1" mold="paging" autopaging="true"
	 	style="border: 2px solid #80ccff; border-radius: 5px;"
			emptyMessage="Este Sub-Proyecto no tiene Isfpp's asignadas." 
				model="@load(vm.instanciasIsfpp)">
		<columns>
			<column label="Titulo" width="32%"/>
			<column label="Estado" width="15%"/>
			<column label="Fecha Inicio" width="16%"/>
			<column label="Fecha Fin" width="16%"/>
			<column label="Ver" width="7%" align="center"/>
			<column label="Editar" width="7%" align="center"/>
			<column label="Baja" width="7%" align="center"/>
		</columns>
		<template name="model">
			<row>
				<label value="@load(each.titulo)"/>
				<label value="@load(each.estado)"/>
				<label value="@load(each.inicio) @converter('formatedDate', format ='dd/MM/yyyy')"/>
				<label value="@load(each.fin) @converter('formatedDate', format='dd/MM/yyyy')"/>
				<toolbarbutton image="recursos/img/icon-preview.png" 
					tooltip="Ver detalle" onClick="@command('verIsfpp', idItem=each.id)" />
				<toolbarbutton image="recursos/img/icon-edit.png" disabled="@load(not vm.editando)"
					tooltiptext="Editar datos" onClick="@command('editarIsfpp', idItem=each.id)" />
				<toolbarbutton image="recursos/img/icon-trash.png" disabled="@load(not vm.editando)"
					tooltiptext="Dar de baja"
					onClick="" />				
			</row>
		</template>
	</grid>
</vbox>